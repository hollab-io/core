Keeping user-created org chart circles and roles only in component state -> Persist custom groups and nodes in localStorage and merge them with the base dataset on mount.
Replacing local mock data with a shared snapshot only under the hood -> Pair integration work with at least one visible product workflow so the user can immediately see the result in the UI.
